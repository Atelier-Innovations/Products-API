// CREATE INDEX styles_id_index ON styles (id);

// CREATE INDEX photos_styles_id ON photos (styles_id);

// CREATE INDEX skus_styles_id ON skus (styles_id);

// CREATE INDEX current_product_index ON related (current_product_id);